# CodeOCR 安装教程

## 目录内容

- `code-ocr/SKILL.md`：CodeOCR skill 主文件。
- `code-ocr.zip`：压缩安装包，内容与 `code-ocr/` 目录一致。
- `README.md`：简要说明。

## 安装方式一：复制目录安装

1. 将 `code-ocr` 文件夹复制到本机 skills 目录：

```powershell
Copy-Item -Recurse -Force .\code-ocr "$env:USERPROFILE\.agents\skills\code-ocr"
```

2. 重启 Codex 或刷新技能列表。

3. 使用时在消息中提到：

```text
[$code-ocr](C:\Users\<你的用户名>\.agents\skills\code-ocr\SKILL.md)
```

## 安装方式二：从压缩包安装

1. 解压 `code-ocr.zip`。

2. 将解压后的 `code-ocr` 文件夹放入：

```text
C:\Users\<你的用户名>\.agents\skills\
```

3. 确认文件存在：

```text
C:\Users\<你的用户名>\.agents\skills\code-ocr\SKILL.md
```

## 触发示例

```text
[$code-ocr](C:\Users\<你的用户名>\.agents\skills\code-ocr\SKILL.md) 识别图中代码，注意左侧行号，不要串行漏行，保存成 .c 文件
```

## 功能说明

CodeOCR 会按截图左侧行号转录代码，保留缩进、空行、花括号和可见的自赋值语句。转录完成后会做行数校对，并对德语注释中的明显乱码进行保守修复。
