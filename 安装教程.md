# Code Explanation 安装教程

本文说明如何安装和使用 `Code Explanation` 技能包。该技能用于为代码目录创建中文逐行注释副本，在副本中保留原始代码行，并在每个非空原始行正上方添加 `注释`、`逻辑` 或 `原注释翻译`。

## 1. 技能包内容

安装包目录结构如下：

```text
Code Explanation/
├─ SKILL.md
├─ 安装教程.md
├─ references/
│  └─ commenting-style.md
└─ scripts/
   └─ validate_annotated_copy.py
```

各文件作用如下：

- `SKILL.md`：技能主说明文件，定义触发条件、工作流、注释格式和验证规则。
- `安装教程.md`：当前安装和使用说明。
- `references/commenting-style.md`：逐行注释风格、德语/英语原注释翻译规则、变量名推测规则和安全边界。
- `scripts/validate_annotated_copy.py`：校验注释副本是否保留原始行，并确认每个非空原始行上方都有指定注释前缀。

## 2. 从 GitHub 获取安装包

目标仓库分支：

```text
https://github.com/xiaoluo-creative/Codex-Skill/tree/Code-Explanation
```

如果使用 Git，可执行：

```powershell
git clone --branch Code-Explanation --single-branch `
  https://github.com/xiaoluo-creative/Codex-Skill.git `
  "Code Explanation"
```

如果不使用 Git，也可以在 GitHub 页面切换到 `Code-Explanation` 分支后下载 ZIP，并将解压后的分支根目录内容作为技能包内容。

## 3. 安装到全局技能目录

Codex 个人技能目录通常为：

```text
C:\Users\<你的用户名>\.agents\skills
```

将整个 `Code Explanation` 文件夹复制到该目录下，目标路径应为：

```text
C:\Users\<你的用户名>\.agents\skills\Code Explanation
```

如果使用 PowerShell，可在下载目录的上一层执行：

```powershell
$SkillRoot = Join-Path $env:USERPROFILE ".agents\skills\Code Explanation"
New-Item -ItemType Directory -Force -Path (Split-Path $SkillRoot) | Out-Null
Copy-Item -LiteralPath ".\Code Explanation" -Destination (Split-Path $SkillRoot) -Recurse -Force
```

如果已经在目标目录中存在旧版本，可先关闭 Codex，再覆盖复制。

## 4. 安装后检查

安装完成后，建议确认以下文件存在：

- `C:\Users\<你的用户名>\.agents\skills\Code Explanation\SKILL.md`
- `C:\Users\<你的用户名>\.agents\skills\Code Explanation\安装教程.md`
- `C:\Users\<你的用户名>\.agents\skills\Code Explanation\references\commenting-style.md`
- `C:\Users\<你的用户名>\.agents\skills\Code Explanation\scripts\validate_annotated_copy.py`

建议关闭并重新启动 Codex，以便刷新技能索引和 `$` 技能列表显示名称。

## 5. 调用方式

如果技能列表中已经显示 `Code Explanation`，可以直接按技能名调用。

如果技能列表中暂时没有显示，也可以使用显式路径调用：

```text
[$Code Explanation](C:\Users\<你的用户名>\.agents\skills\Code Explanation\SKILL.md)
```

示例：

```text
[$Code Explanation](C:\Users\<你的用户名>\.agents\skills\Code Explanation\SKILL.md) 给这个 C 代码文件夹创建中文逐行注释副本
```

## 6. 标准输出效果

普通代码行会变成：

```c
// 注释：说明该行的直接含义，并在变量出现时给出中文释义或合理推测。
// 逻辑：说明该行在算法、控制流程、数据流或安全保护中的作用。
original_line;
```

德语、英语或混合语言原注释会变成：

```c
// 原注释翻译：自然翻译原注释，不改变原注释文本。
// 逻辑：说明这条注释对应的代码块为什么存在、控制目的是什么。
// Original comment line
```

技能默认会创建类似下面的副本目录，并只修改副本：

```text
原代码目录_中文逐行注释副本
```

## 7. 校验脚本使用示例

完成注释后，可以用校验脚本检查副本结构。示例：

```powershell
python "C:\Users\<你的用户名>\.agents\skills\Code Explanation\scripts\validate_annotated_copy.py" `
  "D:\Project\原代码目录" `
  "D:\Project\原代码目录_中文逐行注释副本" `
  --extensions .c
```

如果本机 `python` 不在 `PATH` 中，可使用 Python 的完整路径执行。

校验通过时会输出类似：

```text
OK: validated 9 file(s).
```

## 8. 适用场景

该技能适合处理以下任务：

- 为 `.c`、`.h` 或其他源码文件夹创建中文逐行注释副本。
- 保留原始代码不变，只在副本中增加阅读说明。
- 翻译代码中的德语、英语或混合语言原注释。
- 根据变量名、控制上下文和附近逻辑推测变量中文释义。
- 为 ABS、VMC、MPC、嵌入式控制、状态机、计时器和标定阈值代码补充阅读说明。

## 9. 注意事项

- 原始代码目录不要直接修改，先创建副本再注释。
- 原始代码行必须保持完全不变，不要顺手修正格式、拼写或逻辑。
- 变量含义不确定时使用“推测为”“可能表示”等措辞。
- 添加中文后建议保存为 UTF-8 编码。
- 如果 PowerShell 显示中文乱码，优先用支持 UTF-8 的编辑器查看，或在读取时指定 UTF-8。
