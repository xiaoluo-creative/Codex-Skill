# Word Formula Repair 安装教程

本文说明如何安装和使用 `word-formula-layout-repair` 技能包。该技能用于检查并修复 Word `.docx` 文档中的公式乱码、公式符号异常、公式占位残留以及相关排版问题。

## 1. 技能包内容

安装包目录结构如下：

```text
word-formula-layout-repair/
├─ SKILL.md
├─ LICENSE.txt
├─ 安装教程.md
├─ agents/
│  └─ openai.yaml
├─ assets/
│  ├─ word-formula-layout-repair-small.svg
│  └─ word-formula-layout-repair.png
└─ scripts/
   ├─ inspect_word_omaths.ps1
   └─ repair_word_formula_symbols.ps1
```

各文件作用如下：

- `SKILL.md`：技能主说明文件，定义触发条件、适用范围和标准工作流。
- `agents/openai.yaml`：技能在 Codex 中的展示信息。
- `scripts/inspect_word_omaths.ps1`：检查 Word 文档中的公式对象、线性公式文本和基础异常信息。
- `scripts/repair_word_formula_symbols.ps1`：按公式索引批量修复损坏的公式符号，并输出修复后的新文档。

## 2. 安装到全局技能目录

Codex 默认的全局技能目录通常为：

```text
C:\Users\24131\.codex\skills
```

将整个 `word-formula-layout-repair` 文件夹复制到该目录下，目标路径应为：

```text
C:\Users\24131\.codex\skills\word-formula-layout-repair
```

如果使用 PowerShell，可执行：

```powershell
Copy-Item -LiteralPath "D:\VMCHDC\word-formula-layout-repair" `
  -Destination "C:\Users\24131\.codex\skills\word-formula-layout-repair" `
  -Recurse -Force
```

## 3. 安装后检查

安装完成后，建议确认以下文件存在：

- `C:\Users\24131\.codex\skills\word-formula-layout-repair\SKILL.md`
- `C:\Users\24131\.codex\skills\word-formula-layout-repair\安装教程.md`
- `C:\Users\24131\.codex\skills\word-formula-layout-repair\agents\openai.yaml`
- `C:\Users\24131\.codex\skills\word-formula-layout-repair\scripts\inspect_word_omaths.ps1`
- `C:\Users\24131\.codex\skills\word-formula-layout-repair\scripts\repair_word_formula_symbols.ps1`

建议关闭并重新启动 Codex，以便刷新技能索引。

## 4. 调用方式

如果技能列表中已经显示 `Word Formula Repair`，可以直接按技能名调用。

如果技能列表中暂时没有显示，也可以使用显式路径调用：

```text
[$word-formula-layout-repair](C:\Users\24131\.codex\skills\word-formula-layout-repair\SKILL.md)
```

示例：

```text
[$word-formula-layout-repair](C:\Users\24131\.codex\skills\word-formula-layout-repair\SKILL.md) 检查这个 docx 里的公式乱码和排版问题
```

## 5. 脚本使用示例

### 5.1 检查 Word 公式对象

```powershell
powershell -ExecutionPolicy Bypass -File `
  "C:\Users\24131\.codex\skills\word-formula-layout-repair\scripts\inspect_word_omaths.ps1" `
  -DocPath "D:\VMCHDC\示例文档.docx"
```

### 5.2 修复公式符号乱码

先准备一个修复说明 JSON 文件，例如：

```json
[
  { "index": 25, "linear": "CfVxFltPT1 ≈ T_s/(T_s + τ)" },
  { "index": 26, "linear": "CfVxFltPT1 = 1 - e^(-T_s/τ)" }
]
```

然后执行：

```powershell
powershell -ExecutionPolicy Bypass -File `
  "C:\Users\24131\.codex\skills\word-formula-layout-repair\scripts\repair_word_formula_symbols.ps1" `
  -DocPath "D:\VMCHDC\input.docx" `
  -RepairJsonPath "D:\VMCHDC\repairs.json" `
  -OutputPath "D:\VMCHDC\output_fixed.docx"
```

## 6. 适用场景

该技能适合处理以下问题：

- Word 文档中的公式符号乱码。
- `OMath` 或 `OMaths` 对象损坏。
- 公式占位符残留，例如 `__EQ`。
- 公式修复后需要再次检查排版、页数或公式对象数量。
- 需要按公式索引精确修复 `⇒`、`≈`、`τ` 等特殊符号。

## 7. 注意事项

- 修复公式时，优先输出到新文件，不要直接覆盖正在打开的源文档。
- 如果源文件路径包含中文或其他非 ASCII 字符，脚本会优先使用临时 ASCII 路径完成处理，再回写输出文件。
- 如果 Word 正在占用目标文档，建议先关闭 Word 再执行修复脚本。
- 安装后如果技能列表未立即显示，先重启 Codex 再检查。
